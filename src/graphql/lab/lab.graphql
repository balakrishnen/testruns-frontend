import { gql } from '@apollo/client';

export const GET_LABS = gql`
  query Get_all_labs {
    get_all_labs {
        _id
        name
        organisationID
        status
        departmentId
        isActive
        isDeleted
        createdAt
        updatedAt
    }
}
`;