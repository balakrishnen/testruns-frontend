import { gql } from '@apollo/client';

export const GET_LABS = gql`
  query Get_all_labs {
    get_all_labs {
        _id
        name
        departmentId
        userId
        isActive
        isDeleted
        createdAt
        updatedAt
    }
}
`;